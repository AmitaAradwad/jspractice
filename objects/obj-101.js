let person={
    Name: 'Amita',
    Age: 23,
    location:'Mumbai'
}
// console.log(person.Name)
console.log(`${person.Name} is ${person.Age} and lives in ${person.location}`); 
person.Name='sarika'
console.log(`${person.Name} is ${person.Age} and lives in ${person.location}`); 
